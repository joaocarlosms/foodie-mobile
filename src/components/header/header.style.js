import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    logo: {
        width: 170,
        height: 50
    },

    header: {
        width: "100%",
        alignItems: "center"
    },

    title: {
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}